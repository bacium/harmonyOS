"use strict";
/**
 * 组件状态
 *   @State====>@Prop(通过this传递)
 *   @State====>@Link(通过`$`传递)
 *
 *
 */
struct StateManger extends   {
    constructor(
/**
 * 组件状态
 *   @State====>@Prop(通过this传递)
 *   @State====>@Link(通过`$`传递)
 *
 *
 */



    ) {
        /**
         * 组件状态
         *   @State====>@Prop(通过this传递)
         *   @State====>@Link(通过`$`传递)
         *
         *
         */
    }
    build() {
            .height('100%');
    }
}
// 子组件1----->@Prop传参
struct StateManger_state extends   {
    constructor(
/**
 * 组件状态
 *   @State====>@Prop(通过this传递)
 *   @State====>@Link(通过`$`传递)
 *
 *
 */



    ) {
        /**
         * 组件状态
         *   @State====>@Prop(通过this传递)
         *   @State====>@Link(通过`$`传递)
         *
         *
         */
    }
    build() {
            .width('100%');
    }
}
// 子组件2----->link传参
struct StateManger_link extends  {
    constructor(
/**
 * 组件状态
 *   @State====>@Prop(通过this传递)
 *   @State====>@Link(通过`$`传递)
 *
 *
 */



    /**
     * 组件状态
     *   @State====>@Prop(通过this传递)
     *   @State====>@Link(通过`$`传递)
     *
     *
     */
    ) {
        /**
         * 组件状态
         *   @State====>@Prop(通过this传递)
         *   @State====>@Link(通过`$`传递)
         *
         *
         */
    }
    build() {
        ;
    }
}
//# sourceMappingURL=StateManger.js.map